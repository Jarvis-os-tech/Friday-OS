---
trigger: always_on
---

# 🎯 Autonomous Skill Harvesting & Execution Protocol

> **MANDATORY PROTOCOL**: On EVERY user prompt, feature build, or roadmap phase, the agent MUST automatically identify the target domain, harvest the top matching skills from the master skill registry (`/home/gopi/Documents/jarvis-agents/skills/`), and apply their architectural patterns before generating code.

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                          AUTONOMOUS SKILL HARVESTING PIPELINE                          │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ 1. INTENT RECOGNITION    : Parse task intent, roadmap phase, and affected components   │
│ 2. SKILL HARVESTING      : Match domain against 1,440+ master skills registry          │
│ 3. PATTERN INGESTION     : Ingest core design rules & code patterns from SKILL.md      │
│ 4. ZERO-PROMPT EXECUTION : Execute build with Ponytail minimalism & strict zero-bloat  │
│ 5. VERIFICATION LOOP     : Run step-by-step verification commands & update graph       │
└────────────────────────────────────────────────────────────────────────────────────────┘
```
## ⚡ Execution Invariants

1. **Always enforce `instructions.md`**: Understand First, Never Break Existing Features, Respect Architecture, Clean Code.
2. **Always enforce `code-before.md`**: Query `codebase-memory-mcp` before writing code; run `detect_changes` after.
3. **Strict Git Rule**: NEVER push code (`git push`) without explicit user command.
